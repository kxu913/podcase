import 'package:flutter/material.dart';

const pagePadding = EdgeInsets.only(left: 16, right: 16);
const tagPadding = EdgeInsets.only(top: 8);
const titleSummaryPadding = EdgeInsets.only(top: 10, bottom: 5);
const cardPadding = EdgeInsets.only(bottom: 8, top: 8);
const titlePadding = EdgeInsets.only(top: 24, left: 10, bottom: 8);
const rightPadding = EdgeInsets.only(right: 10);
const inlinePadding = EdgeInsets.only(top: 4, bottom: 4);
// const subtitlePadding = const EdgeInsets.only(top: 8, left: 10, bottom: 6);
